/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '',
  assetPrefix: '',
  reactStrictMode: true,
  trailingSlash: true,
};

export default nextConfig;
